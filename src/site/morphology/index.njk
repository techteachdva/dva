---
title: "Morphology: The Analysis of Words"
permalink: /morphology/
layout: layouts/note.njk
contentClasses: morphology-page
---

<link rel="stylesheet" href="/styles/morphology-garden.css" />

<div class="morphology-page">
  <header class="morphology-top">
    <h1 class="morph-page-title">Morphology: The Analysis of Words</h1>
    <p class="morphology-tagline">
      Highlight reel from Park-Johnson &amp; Shin (2020), <em>Linguistics for Language Teachers</em>, Chapter 4.
      <strong>Open with one lemma</strong>—the intro settles on a random pick; use <strong>Word tree</strong> to steer. Five key teaching ideas stay below once you dive in.
    </p>
  </header>

  <div class="morphology-shell" id="morphology-shell" role="region" aria-label="Morphology diagrams and controls">
    <div
      id="morph-canvas-host"
      class="morph-canvas-host"
      role="img"
      aria-label="Flat morpheme diagrams: isolated tree, linked trees, or compare trees"
    ></div>
    <div class="morph-ui-panel">
      <div class="morph-ui-row morph-ui-row--meta" role="group" aria-label="Vocabulary note">
        <span class="morph-curriculum-note" title="Edit per-word tier tags in morphology-words-data.js">One vocabulary set · tier shown inside each mini-lesson</span>
        <button
          type="button"
          id="morph-tour-btn"
          class="morph-tour-btn"
          aria-label="Start guided tour of controls and canvas"
          title="Guided tour"
        >
          📖
        </button>
        <button
          type="button"
          id="morph-help-btn"
          class="morph-help-btn"
          aria-label="Help: controls and views"
          title="Help"
        >
          ❓
        </button>
      </div>
      <div class="morph-ui-toolbar" role="group" aria-label="Isolated tree, linked trees, or compare trees">
        <div class="morph-toolbar__col morph-toolbar__col--isolate">
          <div class="morph-ui-row morph-ui-row--arrange morph-cluster morph-cluster--isolate" role="group" aria-label="Isolated tree">
            <span class="morph-ui-label">Isolate</span>
            <button
              type="button"
              id="morph-btn-arr-garden"
              class="morph-view-btn morph-view-btn--icon morph-view-btn--active"
              aria-pressed="true"
              title="Isolated Tree"
              aria-label="Isolated Tree"
            >
              🌳
            </button>
          </div>
          <div id="morph-word-row" class="morph-ui-row morph-word-row--isolate">
            <label for="morph-word-select">Focus</label>
            <select id="morph-word-select" class="morph-select" aria-describedby="morph-hint" title="Lemma or morpheme"></select>
          </div>
        </div>
        <div class="morph-toolbar__col morph-toolbar__col--compare">
          <div class="morph-ui-row morph-ui-row--arrange morph-cluster morph-cluster--compare" role="group" aria-label="Compare trees">
            <span class="morph-ui-label">Compare</span>
            <button
              type="button"
              id="morph-btn-arr-compare"
              class="morph-view-btn morph-view-btn--icon"
              aria-pressed="false"
              title="Compare Trees"
              aria-label="Compare Trees"
            >
              ⚖
            </button>
          </div>
          <div id="morph-compare-row" class="morph-compare-stack morph-compare-row morph-ui-row" hidden aria-hidden="true">
            <div class="morph-compare-pair">
              <label for="morph-compare-a">Compare A</label>
              <select id="morph-compare-a" class="morph-select morph-select--compare" title="First word in compare mode"></select>
            </div>
            <div class="morph-compare-pair">
              <label for="morph-compare-b">Compare B</label>
              <select id="morph-compare-b" class="morph-select morph-select--compare" title="Second word in compare mode"></select>
            </div>
          </div>
        </div>
        <div class="morph-toolbar__col morph-toolbar__col--links">
          <div class="morph-ui-row morph-ui-row--arrange morph-cluster morph-cluster--links" role="group" aria-label="Linked trees">
            <span class="morph-ui-label">Links</span>
            <button
              type="button"
              id="morph-btn-arr-master"
              class="morph-view-btn morph-view-btn--icon"
              aria-pressed="false"
              title="Linked Trees"
              aria-label="Linked Trees"
            >
              🔗
            </button>
          </div>
          <div id="morph-links-morpheme-row" class="morph-ui-row morph-links-morpheme-row" hidden aria-hidden="true">
            <label for="morph-links-morpheme">Morpheme</label>
            <select
              id="morph-links-morpheme"
              class="morph-select morph-select--links"
              aria-describedby="morph-hint"
              title="Choose a morpheme to explore linked lemmas"
            ></select>
          </div>
          <div class="morph-ui-row morph-fs-slot">
            <button
              type="button"
              id="morph-btn-fs"
              class="morph-fs-btn morph-fs-btn--icon"
              title="Full screen"
              aria-label="Full screen"
            >
              🖥️
            </button>
          </div>
        </div>
      </div>
      <div
        class="morph-ui-row morph-ui-row--lesson-zoom morph-lesson-zoom-bar"
        role="toolbar"
        aria-label="Mini-lesson text size (duplicate toolbar when the diagram fills the screen)"
      >
        <span class="morph-ui-label">Lesson</span>
        <button
          type="button"
          class="morph-text-size-btn morph-text-size-btn--bar"
          data-morph-lesson-zoom="smaller"
          title="Smaller lesson text ( [ )"
          aria-label="Decrease mini-lesson text size"
        >
          A−
        </button>
        <button
          type="button"
          class="morph-text-size-btn morph-text-size-btn--bar"
          data-morph-lesson-zoom="reset"
          title="Default lesson text size ( Alt+0 )"
          aria-label="Reset mini-lesson text size"
        >
          A
        </button>
        <button
          type="button"
          class="morph-text-size-btn morph-text-size-btn--bar"
          data-morph-lesson-zoom="larger"
          title="Larger lesson text ( ] )"
          aria-label="Increase mini-lesson text size"
        >
          A+
        </button>
      </div>
      <p id="morph-hint" class="morph-hint morph-hint--compact">
        <strong>Tip:</strong> The board is <strong>2D</strong>—drag to pan. 🌳 <strong>Isolated Tree</strong>; 🔗 <strong>Linked Trees</strong>; ⚖ <strong>Compare Trees</strong>. Wheel over the diagram <strong>scrolls the page</strong> when there is room (reach the “Five ideas” section); use <kbd>Ctrl</kbd>/<kbd>⌘</kbd>+wheel to zoom the diagram, or wheel when the page cannot scroll further. In fullscreen, wheel over the docked lesson scrolls <strong>lesson text</strong>. Lesson text size: <strong>A−</strong> <strong>A</strong> <strong>A+</strong> or keys <kbd>[</kbd> <kbd>]</kbd>. Hover spheres for gloss ·
        <kbd>F</kbd> fit · <kbd>R</kbd> reset<span class="visually-hidden"> (Space toggles orbit only in the experimental 3D mode from backslash)</span>.
      </p>
    </div>

    <div
      id="morph-help"
      class="morph-help morph-help--hidden"
      role="dialog"
      aria-modal="true"
      aria-labelledby="morph-help-title"
    >
      <button type="button" class="morph-help__backdrop" aria-label="Close help"></button>
      <div class="morph-help__panel">
        <h2 id="morph-help-title" class="morph-help__title">Digital morphemes — how to use</h2>
        <div class="morph-help__body">
          <section class="morph-help__sec">
            <h3>Vocabulary tiers (Beck-style)</h3>
            <p>
              The site loads <strong>one</strong> vocabulary set. Each lemma is tagged
              <strong>Tier 1</strong> (common everyday),
              <strong>Tier 2</strong> (academic / cross-subject), or
              <strong>Tier 3</strong> (lower-frequency or domain-heavy). The mini-lesson states the tier and points you to
              <code>morphology-words-data.js</code> if you want to reclassify a word.
            </p>
          </section>
          <section class="morph-help__sec">
            <h3>Flat board &amp; Arrange</h3>
            <p class="morph-help__sub">
              The explorer opens after a brief intro cinematic: one lemma takes the spotlight with its mini-lesson. Use <strong>Arrange</strong> when you’re ready for different vantage points on the full vocabulary set.
            </p>
            <ul>
              <li>
                <strong>🌳 Isolated Tree</strong> — One centered diagram with deep mini-lessons and morphology notes.
              </li>
              <li>
                <strong>🔗 Linked Trees</strong> — Shows lemmas that share morphemes with your focus pick (word <em>or</em> morpheme); magenta bridges stay within that visible set.
              </li>
              <li>
                <strong>⚖ Compare Trees</strong> — Two selections side by side plus a unified lesson keyed to overlaps (exit tickets stay word-specific).</li>
              <li>
                <strong>🖥️ Full screen</strong> — Fills the viewport (or immersive mode on mobile Safari) so the diagram and chrome stay legible when teaching.
              </li>
            </ul>
            <p class="morph-help__sub">
              <strong>Maintainer shortcut:</strong> press <kbd>\</kbd> to toggle the legacy <em>orbital 3D</em> camera (not surfaced in the toolbar). Return to the slash key to come back to the default board.
            </p>
          </section>
          <section class="morph-help__sec">
            <h3>Mouse / touch</h3>
            <ul>
              <li><strong>Hover</strong> a morpheme sphere for tooltip glosses, categories, and sibling lemmas that share it.</li>
              <li><strong>Drag</strong> to pan across the board (or orbit when <kbd>\</kbd> 3D mode is on).</li>
              <li>
                <strong>Scroll wheel</strong> over the diagram moves the page when you can still scroll (to reach content below); hold <kbd>Ctrl</kbd> / <kbd>⌘</kbd> while scrolling to zoom the board, or scroll when the page is already at the top or bottom.
              </li>
              <li><strong>Click</strong> a morpheme to ease the camera toward that node.</li>
              <li><strong>Double-click</strong> refits the framing so the surrounding word tree dominates the view.</li>
              <li><strong>Shift + double-click</strong> (3D mode only) toggles slow auto-orbit around the focused node.</li>
            </ul>
          </section>
          <section class="morph-help__sec">
            <h3>Keyboard</h3>
            <ul>
              <li><kbd>W</kbd> <kbd>A</kbd> <kbd>S</kbd> <kbd>D</kbd>, arrows — glide / orbit (only in experimental 3D via <kbd>\</kbd>).</li>
              <li><kbd>F</kbd> — fit isolate, linked, or dual layout into view.</li>
              <li><kbd>R</kbd> — reset framing for the active arrange mode.</li>
              <li><kbd>Space</kbd> — toggle orbit drift while 3D experimental mode is enabled.</li>
              <li><kbd>\\</kbd> — toggle the hidden spatial 3D camera; default teaching mode stays flat.</li>
              <li><kbd>Escape</kbd> — leave this help drawer, immersive mode, or fullscreen.</li>
              <li><kbd>[</kbd> / <kbd>]</kbd> — decrease / increase mini-lesson text size when a lesson is showing (not while typing in a control).</li>
              <li><kbd>Alt</kbd>+<kbd>0</kbd> — reset lesson text size to default.</li>
            </ul>
          </section>
          <section class="morph-help__sec">
            <h3>Focus menu &amp; details</h3>
            <p>
              <strong>Focus</strong> lists lemmas and morpheme tags. Isolated Tree uses one lemma’s diagram; Linked Trees fans out every lemma that matches your word or morpheme pick. Compare Trees swaps in two selectors (each can be a word or a morpheme).
            </p>
            <p class="morph-help__sub">
              <strong>Mini-lesson text size:</strong> use <strong>A−</strong> / <strong>A</strong> / <strong>A+</strong> above the lesson (inline on the page and when docked in full screen). Keys <kbd>[</kbd> smaller and <kbd>]</kbd> larger; <kbd>Alt</kbd>+<kbd>0</kbd> resets. Your choice is saved in this browser.
            </p>
          </section>
        </div>
        <button type="button" class="morph-help__close">Close</button>
      </div>
    </div>
  </div>

  <div id="morph-detail" class="morph-detail">
    <p id="morph-lesson-zoom-live" class="visually-hidden" aria-live="polite" aria-atomic="true"></p>
    <div
      id="morph-detail-text-toolbar"
      class="morph-detail__text-tools"
      role="toolbar"
      aria-label="Mini-lesson text size"
      aria-controls="morph-detail-body"
    >
      <span id="morph-detail-text-tools-label" class="morph-detail__text-tools-label">Lesson text</span>
      <button type="button" id="morph-lesson-text-smaller" class="morph-text-size-btn" title="Smaller text (keyboard [ )" aria-label="Decrease mini-lesson text size">A−</button>
      <button type="button" id="morph-lesson-text-reset" class="morph-text-size-btn" title="Default size (Alt+0)" aria-label="Reset mini-lesson text size">A</button>
      <button type="button" id="morph-lesson-text-larger" class="morph-text-size-btn" title="Larger text (keyboard ] )" aria-label="Increase mini-lesson text size">A+</button>
    </div>
    <div id="morph-detail-body" class="morph-detail__body" aria-live="polite"></div>
  </div>

  <section class="morph-highlights" aria-labelledby="morph-highlights-title">
    <h2 id="morph-highlights-title" class="morph-highlights-title">Five ideas from the chapter</h2>
    <p class="morph-highlights-intro">
      These are the chapter’s through-lines for teachers—not section titles. Each one is illustrated somewhere in the
      trees above.
    </p>
    <ol class="morph-concept-list">
      <li class="morph-concept">
        <h3 class="morph-concept-name">Morpheme</h3>
        <p>
          The <strong>smallest unit</strong> with meaning or grammatical work. Words can be <strong>simple</strong> (one
          morpheme, e.g. <em>happy</em>) or <strong>complex</strong> (e.g. <em>teacher</em> = <em>teach</em> +
          <em>-er</em>). <strong>Syllables ≠ morphemes.</strong>
        </p>
      </li>
      <li class="morph-concept">
        <h3 class="morph-concept-name">Root and affix</h3>
        <p>
          The <strong>root</strong> carries the core meaning; <strong>affixes</strong> attach as
          <strong>prefixes</strong> or <strong>suffixes</strong>. <strong>Compounds</strong> glue whole words together
          (e.g. <em>hallway</em>, <em>rainbow</em>). The chapter also shows <strong>infixes</strong> and
          <strong>circumfixes</strong> in other languages so English isn’t the whole story.
        </p>
      </li>
      <li class="morph-concept">
        <h3 class="morph-concept-name">Free, bound, content, function</h3>
        <p>
          <strong>Free morphemes</strong> stand alone; <strong>bound morphemes</strong> must attach.
          <strong>Content</strong> morphemes fill open classes (nouns, verbs, adjectives, adverbs);
          <strong>function</strong> morphemes belong to small closed classes (articles, prepositions, pronouns).
          <strong>Bound roots</strong> (e.g. in <em>inept</em>) remind us that not every stem is a free word.
        </p>
      </li>
      <li class="morph-concept">
        <h3 class="morph-concept-name">Derivation vs. inflection</h3>
        <p>
          <strong>Derivational</strong> affixes build new words (often changing class or meaning) and are not required
          by syntax. <strong>Inflectional</strong> suffixes mark grammar (plural <em>-s</em>, tense <em>-ed</em>, etc.),
          stay on the <strong>outer edge</strong>, and don’t change basic word class. Order matters:
          derivation tends to sit <strong>inside</strong> inflection.
        </p>
      </li>
      <li class="morph-concept">
        <h3 class="morph-concept-name">Allomorphs, analysis, teaching</h3>
        <p>
          Morphemes have <strong>predictable surface variants</strong> (e.g. plural <em>-s</em>, past <em>-ed</em>,
          <em>a</em> ~ <em>an</em>). <strong>Structure</strong> can differ even when spelling matches—compare the two
          <em>unlockable</em> trees. For classrooms: make <strong>word families</strong> and affix meanings explicit so
          learners recognize patterns in new vocabulary.
        </p>
      </li>
    </ol>
    <p class="morph-source-line">
      <strong>Source:</strong> Park-Johnson, S., &amp; Shin, S. J. (2020). <em>Linguistics for Language Teachers</em>.
      Taylor &amp; Francis. ·
      <strong>Further reading:</strong> Aronoff, M., &amp; Fudeman, K. (2010). <em>What is morphology?</em> (2nd ed.).
      Wiley-Blackwell.
    </p>
  </section>

  <section class="morph-chart" aria-labelledby="morph-chart-title">
    <h2 id="morph-chart-title" class="morph-chart__title">Word bank morpheme chart</h2>
    <p class="morph-chart__sub">
      Click a <strong>morpheme</strong> to see all lemmas in the bank that share it. Click a <strong>word</strong> to jump the viewer.
      (Edit origins/meanings and Wiktionary links in <code>morphology-morpheme-catalog.js</code>.)
    </p>
    <div id="morph-morpheme-chart" class="morph-chart__mount" aria-live="polite"></div>
  </section>
</div>

<script type="importmap">
{
  "imports": {
    "three": "https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js",
    "three/addons/": "https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/"
  }
}
</script>
<script type="module" src="/scripts/morphology-garden.js"></script>
